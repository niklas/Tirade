page.update_toolbox(
                   :header => "#{@model.class_name} (#{@model.id})",
                   :content => {:partial => "/#{@model.table_name}/show", :object => @model}
                   )

