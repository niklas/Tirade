page.update_toolbox(
                   :header => "Edit #{current_model.class_name} (#{current_model.id})",
                   :content => {:partial => "/#{current_model.table_name}/form", :object => current_model}
                   )
