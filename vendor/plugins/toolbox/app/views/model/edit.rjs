page.update_toolbox(
                   :header => "Edit #{@model.class_name} (#{@model.id})",
                   :content => {:partial => "/#{@model.table_name}/form", :object => @model}
                   )
