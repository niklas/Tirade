<%= job.title %>
<% if job.has_arguments? %>
  <%= job.arguments.inspect %>
<% end %>
<% if job.locked_at? %>
  (running since <%= time_ago_in_words job.locked_at %>)
<% else %>
  (waiting <%= time_ago_in_words job.run_at %>)
<% end %>
