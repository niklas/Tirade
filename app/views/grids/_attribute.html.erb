<% grid ||= attribute || @rendering.andand.grid || @grid %>
<%= 
grid_tree_list_from(grid.root) do |g|
  content_tag( :span, g.name, :class => (g==grid ? 'active' : '')) + '  ' + 
    edit_grid_link(g, :label => 'edit')
end
%>

