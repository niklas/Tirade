page.update_toolbox(
                   :header => "#{@content.class} '#{@content.title || 'Content'}'",
                   :content => {:partial => '/contents/show', :object => @content},
                   :nav => 'contents'
                   )

