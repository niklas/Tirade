<% content_for :head do %>
  <base href="<%= url_for(:path => @page.path, :trailing_slash => !@page.path.empty?) -%>" />
<% end %>
<%= render_page(@page) %>
<%= render :partial => '/shared/loginout' unless exporting? %>
