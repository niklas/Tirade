<%= warning_tag 'Editing this grid may change the appearance of other pages' %>
<%= error_messages_for :grid %>
<% update_grid_form(@grid) do |f| %>
	<p>
		<label for="yui">Gridtype</label>
		<%= f.select :yui, Grid::Types.invert %>
	</p>
  <p>
  	<%= submit_tag _('Apply'), :class => 'submit' %> or <%= show_rendering_link(@rendering, :label => 'cancel') %>
	</p>
<% end %>
