<% clipboard ||= index || @clipboard || Clipboard.new(session) %>
<% clipboard.items_by_type do |klass, items| %>
  <% items.each do |item| %>
    <%= list_item item %>
  <% end %>
<% end %>

<ul class="actions">
  <%= render :partial => '/news_folders/actions' %>
</ul>
